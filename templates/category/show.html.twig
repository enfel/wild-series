{% extends 'base.html.twig' %}

{% block body %}
    <h1>Toutes les séries de la gategory :{{ categoryName }}</h1>
        {% for program in programs %}

            <h2>{{ loop.index }} / {{ program.title }}</h2>
            <p>{{ program.summary }}</p>
            <a href="{{ path("program_show", { id: program.id }) }}">Voir la fiche</a>
            {% endfor %}
{% endblock %}
